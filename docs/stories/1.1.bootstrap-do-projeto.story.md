# Story 1.1 — Bootstrap do Projeto e Deploy Inicial

## Status

Draft

## Story

**As a** Lucas (desenvolvedor solo),
**I want** inicializar o projeto Next.js 16 com TypeScript estrito, Tailwind 4, ESLint, Prettier e fazer o primeiro deploy no Vercel,
**so that** exista uma base técnica versionada, lintada e deployada antes de qualquer linha de código de produto.

## Acceptance Criteria

1. Repositório Git inicializado em `~/Projetos/escritos-da-geo` com primeiro commit contendo `README.md` e `.gitignore` apropriado para Next.js.
2. Projeto Next.js 16 com App Router configurado, usando TypeScript estrito (`strict: true`, `noUncheckedIndexedAccess: true`).
3. Tailwind CSS 4 configurado com `tailwind.config.ts` mínimo e `globals.css` importado no `app/layout.tsx`.
4. ESLint configurado com preset `next/core-web-vitals` + `eslint-plugin-jsx-a11y`. Prettier configurado e integrado ao ESLint.
5. Pre-commit hook (via `lefthook` ou `husky`) executa `lint` + `typecheck` + `format` antes de permitir commit.
6. Repositório conectado ao Vercel; deploy automático em branch `main` retorna HTTP 200 na URL provisória `escritos-da-geo.vercel.app`.
7. Página inicial (`app/page.tsx`) renderiza placeholder textual `"Escritos da Geo — em construção"` (sem styling final).
8. `.env.local.example` criado com lista de variáveis previstas (vazias, sem valores reais).
9. `CLAUDE.md` na raiz com instruções iniciais para AI agents (escopo, stack, convenções).

## Tasks / Subtasks

- [x] **Task 1 — Inicialização Git e estrutura base** (AC: 1) ✅
  - [x] `git init -b main` em `~/Projetos/escritos-da-geo`
  - [x] Criar `.gitignore` Next.js-aware (inclui Supabase, Sentry, IDE, OS)
  - [x] Criar `README.md` minimalista apontando para docs
  - [x] Primeiro commit `fb00c03`: `chore: initial project structure`

- [x] **Task 2 — Setup Next.js 16 + TypeScript estrito** (AC: 2) ✅
  - [x] Inicializado via `pnpm create next-app@latest .` (Next 16.2.4, React 19.2.4, TS 5.9.3)
  - [x] tsconfig endurecido: `strict` + `noUncheckedIndexedAccess` + `forceConsistentCasingInFileNames` + target ES2022
  - [x] Versões confirmadas: Next 16.2.4 (≥16.x ✓), React 19.2.4 (≥19 ✓), TS 5.9.3 (≥5.6 ✓)
  - [x] `pnpm exec tsc --noEmit` retorna EXIT=0 (strict passou)

- [x] **Task 3 — Tailwind CSS 4 + tokens base** (AC: 3) ✅
  - [x] Tailwind 4.2.4 instalado via create-next-app (com `@tailwindcss/postcss`)
  - [x] **Decisão técnica:** NÃO criar `tailwind.config.ts` — Tailwind 4 dispensou config JS por padrão; toda customização vive em `app/globals.css` via `@theme`. AC original (do PRD/Architecture) refletia padrão Tailwind 3; adaptado à realidade do Tailwind 4.
  - [x] `globals.css` importado em `app/layout.tsx` (default do scaffolding); contém `@import "tailwindcss"` + tokens iniciais via `@theme`

- [ ] **Task 4 — ESLint + Prettier + Hooks** (AC: 4, 5)
  - [ ] Instalar `eslint-plugin-jsx-a11y`, `prettier`, `eslint-config-prettier`, `eslint-plugin-prettier`
  - [ ] Configurar `.eslintrc.json` com presets + jsx-a11y
  - [ ] Criar `.prettierrc` e `.prettierignore`
  - [ ] Instalar e configurar `lefthook` com pre-commit: `lint` + `typecheck` + `format:check`
  - [ ] Adicionar scripts em `package.json`: `lint`, `typecheck`, `format`, `format:check`

- [ ] **Task 5 — Página placeholder** (AC: 7)
  - [ ] Editar `app/page.tsx` para renderizar `"Escritos da Geo — em construção"`
  - [ ] Validar `pnpm dev` exibe corretamente em `localhost:3000`

- [ ] **Task 6 — `.env.local.example`** (AC: 8)
  - [ ] Criar `.env.local.example` com placeholders (sem valores reais):
    - `NEXT_PUBLIC_SUPABASE_URL`
    - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
    - `SUPABASE_SERVICE_ROLE_KEY`
    - `ALLOWED_EMAIL`
    - `SENTRY_DSN`
    - `NEXT_PUBLIC_SENTRY_DSN`

- [ ] **Task 7 — `CLAUDE.md` para AI agents** (AC: 9)
  - [ ] Documentar: escopo do produto, stack, convenções de código, comandos úteis, AC não-óbvios
  - [ ] Incluir referência aos docs (`brief.md`, `prd.md`, `architecture.md`)

- [ ] **Task 8 — Setup Vercel + GitHub** (AC: 6) ⚠️ **REQUER AÇÃO MANUAL DO LUCAS**
  - [ ] Criar repositório no GitHub: `lucasrodrigues/escritos-da-geo` (privado)
  - [ ] Adicionar remote: `git remote add origin git@github.com:lucasrodrigues/escritos-da-geo.git`
  - [ ] Push inicial: `git push -u origin main`
  - [ ] Conectar repositório ao Vercel (via dashboard `vercel.com/new`)
  - [ ] Validar deploy automático: GET na URL `*.vercel.app` retorna HTTP 200

- [ ] **Task 9 — Validação final**
  - [ ] `pnpm install` limpo
  - [ ] `pnpm lint` passa
  - [ ] `pnpm typecheck` passa
  - [ ] `pnpm dev` sobe sem warnings
  - [ ] `pnpm build` completa com sucesso
  - [ ] Pre-commit hook bloqueia commit com lint error proposital (e libera após fix)

## Dev Notes

### Documentos de referência (NÃO carregar a menos que necessário)
- `docs/brief.md` — Project Brief (contexto estratégico)
- `docs/prd.md` — PRD completo (Story 1.1 está na Seção 6 → Epic 1)
- `docs/architecture.md` — Architecture (Seção 3 Tech Stack, Seção 12 Project Structure, Seção 17 Coding Standards)

### Decisões já travadas (Architecture)
- **Package manager:** `pnpm` (não npm/yarn)
- **Node version:** ≥ 20.x (recomendado 22.x)
- **Repo structure:** Monorepo simples, sem turborepo/workspaces no MVP
- **Pre-commit:** `lefthook` (não husky — mais leve)
- **CSS:** Tailwind 4 + tokens via CSS custom properties em `styles/tokens.css` (criados na Story 1.3)
- **Coding standards:** ver `architecture.md` Seção 17 (12 regras críticas)

### Pré-requisitos externos (devem existir antes de Task 8)
- ✅ Conta GitHub do Lucas com permissão para criar repos
- ✅ Conta Vercel do Lucas conectada à GitHub
- ⚠️ `escritosdageo.com.br` NÃO é necessário nesta story (configurado em Story 1.5)

### Constraint: nada de código de produto nesta story
Esta story entrega apenas **infra fundacional**. Nada de Supabase (Story 1.2), nada de identidade visual final (Story 1.3), nada de auth (Story 1.4). A página placeholder é texto puro, sem styling além do default do create-next-app.

## Testing

Sem testes automatizados nesta story (escopo é infra). Validação manual via Task 9.

## Dev Agent Record

### Agent Model Used
- Claude Opus 4.7 (`claude-opus-4-7`) — agent `@dev` (Dex)

### Debug Log References
- Task 1: branch `main` criada via `git init -b main`; commit inicial `fb00c03` contém .gitignore + README.md + 4 docs (3.756 linhas).

### Completion Notes List
- **Task 1 (2026-04-26):** Git repo inicializado limpo em `main`. .gitignore inclui paths Supabase + Sentry + AI agents (`.aiox/`) além do padrão Next.js. README aponta para os 4 docs estratégicos.
- **Task 2-3 (2026-04-26):** create-next-app ofereceu Next 16.2.4 + React 19.2.4 + TS 5.9.3 + Tailwind 4.2.4 — todas as versões mínimas atendidas. Próprios `README.md` e `.gitignore` restaurados após scaffolding (Next sobrescreveu com defaults). tsconfig endurecido com `noUncheckedIndexedAccess` e `forceConsistentCasingInFileNames`; target subido para ES2022. **Tailwind 4 dispensou `tailwind.config.ts`** — toda customização agora via `@theme` no CSS; AC adaptado.
- **Descoberta importante:** Next 16 gerou automaticamente `AGENTS.md` (com aviso "This is NOT the Next.js you know — APIs differ from training data, read `node_modules/next/dist/docs/` first") e `CLAUDE.md` (1 linha: `@AGENTS.md`). Convenção nova de Next 16. Vou ENRIQUECER esses arquivos na Task 7 (não substituir).

### File List
**Created:**
- `.gitignore` (custom — merge entre Next default e extras Supabase/Sentry/AI)
- `README.md` (custom — aponta para docs estratégicos)
- `app/favicon.ico`, `app/globals.css`, `app/layout.tsx`, `app/page.tsx` (Next scaffolding)
- `eslint.config.mjs` (Next ESLint 9 flat config)
- `next.config.ts` (TypeScript config — Next 16 default)
- `next-env.d.ts` (Next ambient types)
- `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`
- `postcss.config.mjs` (Tailwind 4 via PostCSS)
- `public/file.svg`, `public/globe.svg`, `public/next.svg`, `public/vercel.svg`, `public/window.svg`
- `tsconfig.json` (endurecido manualmente)
- `AGENTS.md` (auto-gerado por Next 16 — aviso para AI agents)
- `CLAUDE.md` (auto-gerado por Next 16 — `@AGENTS.md` import)

**Pre-existed (incluídos em commits anteriores):**
- `docs/brief.md`, `docs/prd.md`, `docs/architecture.md`, `docs/stories/1.1.bootstrap-do-projeto.story.md`

### Change Log

| Date | Author | Change |
|------|--------|--------|
| 2026-04-26 | Dex (`@dev`) | Story 1.1 materializada a partir do PRD (atalho greenfield, sem `@sm` formal) |
